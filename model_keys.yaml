# Model API keys and permissions
# These keys are used to authenticate models when they make API calls

models:
  gpt-4-0613:
    key: "gpt4-test-key"
    description: "GPT-4 API (June 2023)"
    allowed_tools:
      - "*"  # Allow all tools
    can_propose: true  # Can propose policy changes
    
  claude-3-opus:
    key: "claude3-test-key"
    description: "Claude 3 Opus API"
    allowed_tools:
      - "calendar.*"
      - "search.*"
      - "email.*"
  
  gemini-pro:
    key: "gemini-test-key"
    description: "Gemini Pro API"
    allowed_tools:
      - "calendar.*"
      - "search.*"
    
  llama-3-70b:
    key: "llama3-test-key"
    description: "Llama 3 (70B) API"
    allowed_tools:
      - "search.*"